<Window x:Class="DEMOex.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DEMOex"
        mc:Ignorable="d" MinHeight="500" MinWidth="400" MaxWidth="400" MaxHeight="500"
        Title="AuthWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource MainGreen}" >
            <Image
            HorizontalAlignment="Center"
            Source="\Recources\logo.png"  
            Margin="10"
            />
        </Grid>

        <Frame Name="AuthFrame" Grid.Row="1" NavigationUIVisibility="Hidden">
            
        </Frame>

        <Grid Grid.Row="2" Background="{StaticResource MainGreen}"/>

    </Grid>
</Window>
